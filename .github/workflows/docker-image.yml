name: Docker Image CI

on:
  push:
    branches: [ feature/container ]
  pull_request:
    branches: [ feature/container ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Set Time image tag
      run: echo '::set-env name=TIME_TAG::$(date +%s)'
    - name: Build the Docker image
      run: docker build . --file Dockerfile --tag ${{SECRETS.REGISTRY_NAME}}/microsoft-graph/microsoftgraph-aspnetcore-webhooks:$TIME_TAG --tag ${{SECRETS.REGISTRY_NAME}}/microsoft-graph/microsoftgraph-aspnetcore-webhooks:latest
    - name: Login to the registry
      run: docker login ${{SECRETS.REGISTRY_NAME}} -u ${{SECRETS.REGISTRY_LOGIN}} -p ${{SECRETS.REGISTRY_PASSWORD}}
    - name: Push image
      run: docker push ${{SECRETS.REGISTRY_NAME}}/microsoft-graph/microsoftgraph-aspnetcore-webhooks:latest
    - name: Push image
      run: docker push ${{SECRETS.REGISTRY_NAME}}/microsoft-graph/microsoftgraph-aspnetcore-webhooks:$TIME_TAG
